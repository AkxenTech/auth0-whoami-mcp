{
  "name": "auth0-whoami-mcp",
  "version": "1.0.0",
  "private": true,
  "description": "An Auth0 Custom Extension MCP server that reports the authenticated caller.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "esbuild src/index.mjs --bundle --platform=node --target=node22 --format=cjs --outfile=build/bundle.js",
    "test": "node --test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "jose": "^6.1.3"
  },
  "devDependencies": {
    "esbuild": "^0.27.0"
  }
}
